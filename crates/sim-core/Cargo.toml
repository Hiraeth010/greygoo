[package]
name = "sim-core"
version = "0.1.0"
edition = "2021"

# Deliberately dependency-free and integer-only so this crate can be dropped
# into a Solana/Anchor program later with minimal changes (no_std-friendly logic).
[dependencies]
